# RMSprop optimizer (1 point)

Implement the RMSprop optimizer by subclassing `torch.optim.Optimizer`.

The implementation must match `torch.optim.RMSprop` for the tested learning
rate, alpha, epsilon, weight decay, momentum and centered options.

