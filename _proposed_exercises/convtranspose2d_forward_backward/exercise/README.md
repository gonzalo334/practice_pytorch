# ConvTranspose2d forward and backward (1 point)

Implement the forward and backward pass of a 2D transposed convolution using
`torch.autograd.Function`.

The implementation must return the same outputs and gradients as
`torch.nn.ConvTranspose2d` for the tested stride, padding, dilation, groups and
output padding values.

