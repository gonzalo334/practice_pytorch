# Conv3D With Unfold And Fold (2 points)

Complete `unfold3d`, `fold3d`, and the forward and backward passes of
`Conv3d` in the `src.conv3d` module. The implementation must build the
convolution from tensor reshaping, indexing, and matrix multiplications.
No `nn` convolution functions are allowed in the implementation.
