# NAdam (1 point)

This class is contained in the `src.optimization` module. Complete the constructor and `step` method. It is not allowed to use methods such as `add` or `sub`, only `+`, `-`, `*` and `/`. It is recommended to look at the steps of the algorithm in the PyTorch documentation.
