# Conv2d Forward (5 points)

Implement the forward pass of `Conv2d` in the `src.conv` module. You cannot use the `nn` package, `fold`, or `unfold`. Only two `for` loops are allowed, and they must iterate over the spatial dimensions. The remaining work should use indexing and tensor operations.
