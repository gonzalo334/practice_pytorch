# HuberLoss (5 points)

Implement the forward and backward passes of `HuberLoss` in the `src.huber_loss` module. You cannot use loops, the `nn` package, or `torch.where`; use masks and indexing instead.

The points are divided into forward pass (2 points) and backward pass (3 points).
