# Adagrad (2 points)

This class is contained in the `src.optimization` module. Complete the constructor and `step` method. It is not allowed to use methods such as `add` or `sub`, only `+`, `-`, `*` and `/`.
