# Residual With PReLU (1.5 points)

Implement the forward and backward methods of the residual layer in the `src.models` module without using the `nn` package. The layer applies a linear transformation, passes it through PReLU, and adds the original input as the residual connection.
