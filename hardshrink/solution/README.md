# Hardshrink (4 points)

Implement the forward and backward passes of the `Hardshrink` layer in the `src.hardshrink` module. You cannot use loops, the `nn` package, or `torch.where`; use masks and indexing instead.

The points are divided into forward pass (1.5 points) and backward pass (2.5 points).
